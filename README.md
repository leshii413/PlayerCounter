# PlayerCounter
BeamMP server-side player analytics plugin that tracks daily, monthly, yearly, and all-time unique users, join sessions, new vs returning players, and month-over-month retention using permanent BeamMP IDs.
